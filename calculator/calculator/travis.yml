language: java
java:
-"stable"
script:stage('Test') {
            steps {
                sh 'mvn AppTest'
            }
            post {
                always {
                    junit 'pom.xml'
                }
            }
        }
