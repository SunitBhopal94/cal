language: java
java:
-"stable"
script:stage('Test') {
            steps {
                sh 'mvn testcal'
            }
            post {
                always {
                    junit 'pom.xml'
                }
            }
        }
